import { Meta, Story, Props, Canvas } from '@storybook/addon-docs/blocks'
import { config, withDesign } from 'storybook-addon-designs'

import Dropdown from '@baserow/modules/core/components/Dropdown'
import DropdownItem from '@baserow/modules/core/components/DropdownItem'

<Meta
    title="Baserow/Dropdown"
    component={Dropdown}
    parameters={{
        backgrounds: {
            default: 'white',
            values: [
                { name: 'white', value: '#ffffff' },
                { name: 'light', value: '#eeeeee' },
                { name: 'dark', value: '#222222' },
            ],
        },
    }}
    decorators={[withDesign]}
    argTypes={{}}
/>

# Chips

export const Template = (args, { argTypes }) => ({
    components: { Dropdown, DropdownItem },
    props: Object.keys(argTypes),
    template: `
    <Dropdown v-bind="$props">
        <DropdownItem name="Hex" value="hex"></DropdownItem>
        <DropdownItem name="RGB" value="rgb"></DropdownItem>
    </Dropdown>
    `,
})

export const designConfig = {
    type: 'figma',
    url: 'https://www.figma.com/file/W7R2rQW7ohsZMeHRfEcPFW/Design-Library?node-id=453:3515&mode=dev',
}

<Canvas>
    <Story
        name="Dropdown"
        parameters={{
            design: config(designConfig),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Example

```javascript
<Dropdown>
    <DropdownItem name="Hex" value="hex"></DropdownItem> <DropdownItem
        name="RGB"
        value="rgb"
    ></DropdownItem>
</Dropdown>
```

## Props

<Props of={Dropdown} />
